spring:
  datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/empresa?useUnicode=true&characterEncoding=utf8&useSSL=false&useLegacyDatetimeCode=false&serverTimezone=UTC
      username: root
      password: root 
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
    hibernate:
      use-new-id-generator-mappings: false
  mvc:
    view:

      suffix: .jsp
      prefix: /WEB-INF/views/
  security:
    user:
      name: pepe
      password: 1234
      
  #data:
    #rest:
      #base-path: /api

